node {
    stage('One') {
        sh 'echo `date`; ping -c 20 localhost'
        sh 'echo `date`; ping -c 20 localhost'
        sh 'echo `date`; ping -c 20 localhost'
    }
    stage('Two') {
        sh 'echo `date`; ping -c 20 localhost'
        sh 'echo `date`; ping -c 20 localhost'
        sh 'echo `date`; ping -c 20 localhost'
    }
    stage('Three') {
        sh 'echo `date`; ping -c 20 localhost'
        sh 'echo `date`; ping -c 20 localhost'
        sh 'echo `date`; ping -c 20 localhost'
    }
    stage('Four') {
        sh 'echo `date`; ping -c 20 localhost'
        sh 'echo `date`; ping -c 20 localhost'
        sh 'echo `date`; ping -c 20 localhost'
    }
    stage('Five') {
        sh 'echo `date`; ping -c 20 localhost'
        sh 'echo `date`; ping -c 20 localhost'
        sh 'echo `date`; ping -c 20 localhost'
    }
}